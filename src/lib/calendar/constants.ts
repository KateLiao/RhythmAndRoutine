/** 时间轴左侧刻度列宽度（px） */
export const TIME_COLUMN_WIDTH = 64;

/** 每小时对应像素高度 */
export const HOUR_HEIGHT = 64;

/** 日程块最小高度（px），15 分钟块可见 */
export const MIN_EVENT_HEIGHT = 24;

/** 日/周视图时间轴起始小时 */
export const TIMELINE_START_HOUR = 7;

/** 日/周视图时间轴结束小时 */
export const TIMELINE_END_HOUR = 24;

/** 时间轴末端分钟（含 24:30 刻度） */
export const TIMELINE_END_MINUTES = 24 * 60 + 30;

/** 拖拽/拉伸吸附粒度（分钟） */
export const SNAP_MINUTES = 15;

/** 日程块最短时长（分钟） */
export const MIN_BLOCK_MINUTES = 15;

/** 周视图日期头高度（px） */
export const DAY_HEADER_HEIGHT = 72;

/** 日历工具栏高度（px） */
export const TOOLBAR_HEIGHT = 64;

/** 侧滑详情面板宽度占比 */
export const DETAIL_DRAWER_WIDTH_RATIO = 0.35;

/** 侧滑动效时长（ms） */
export const DRAWER_TRANSITION_MS = 260;
